Debug mode: certain output (ai's available moves, move history, etc.) not visible unless in debug mode
File output:
 - Game Statistics
 - Debug output
Promotion:
 - Validation of input
Move Struct:
 - do we need color?
Board:
 - do we want upper/lowercase letters still?
Strategy:
 - Checkmate strategy
 - limit openings
Stalemate being treated as a checkmate
Clean up stuff with move.c, plenty of unneeded stuff with new move struct